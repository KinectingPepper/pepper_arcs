from setuptools import setup

setup(name='pepper_arcs',
      version='0.1',
      author='Laura de Koning',
      packages=['pepper_arcs'],
      install_requires=['numpy', 'pandas'],
      zip_safe=False
)